@{
    // Define o Layout padrão para todas as Razor Pages nesta Área e subdiretórios.
    // O caminho deve ser absoluto para garantir que ele encontre o layout principal.
    Layout = "/Views/Shared/_Layout.cshtml";
}