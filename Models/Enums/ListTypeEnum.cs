namespace UnitCheck.Models.Enums
{
    // Define o tipo de lista que será gerada.
    public enum ListType
    {
        // Lista destinada a ser marcada digitalmente (via aplicativo).
        Digital = 1,
        
        // Lista destinada à impressão (em branco, para marcação manual).
        Blank = 2
    }
}